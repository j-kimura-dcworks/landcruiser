{
  "name": "next-landcruiser",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "start": "pnpm -r run start",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}